{
  "name": "typeanimationportfolio",
  "version": "1.0.0",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rd /s /q dist",
    "build:parcel": "parcel build src/index.html --no-content-hash --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/misalagp/TypingAnimation.git"
  },
  "keywords": [],
  "author": "MisalaGP",
  "license": "MIT",
  "homepage": "https://misala.de/",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "splitting": "^1.0.6"
  }
}
